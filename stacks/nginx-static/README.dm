Stack Environment Variables
```
DOMAIN=subdomain.example.domain, use filebrowser at files-subdomain.example.com to place static content. Be sure to  setupa password, an disable filebrowser when not in use.
NODE=manager, worker1, etc
STACK=name of this stack for traefik reference
```
